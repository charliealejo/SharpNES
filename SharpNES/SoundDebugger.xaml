<Window x:Class="SharpNES.SoundDebugger"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SharpNES"
        mc:Ignorable="d"
        Title="SoundDebugger" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="APU Register Writes Log" 
           FontSize="16" FontWeight="Bold" 
           Margin="10" HorizontalAlignment="Center"/>

        <!-- Main text area for APU logs -->
        <ScrollViewer Grid.Row="1" Margin="10">
            <TextBox x:Name="APULogTextBox" 
             IsReadOnly="True"
             TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             FontFamily="Consolas"
             FontSize="12"
             Background="Black"
             Foreground="Lime"
             Text="APU Register Write Log - Ready&#x0A;"/>
        </ScrollViewer>

        <!-- Control buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
            HorizontalAlignment="Right" Margin="10">
            <Button x:Name="ClearLogButton" Content="Clear Log" 
            Width="80" Height="30" Margin="5"
            Click="ClearLogButton_Click"/>
            <Button x:Name="SaveLogButton" Content="Save Log" 
            Width="80" Height="30" Margin="5"
            Click="SaveLogButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
